apiVersion: v1
kind: Service
metadata:
  name: qdrant
  namespace: vartmap
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 6333
      targetPort: http
    - name: grpc
      port: 6334
      targetPort: grpc
  selector:
    app.kubernetes.io/name: qdrant
