apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: whatsapp-gateway-pdb
  namespace: vartmap
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: whatsapp-gateway
